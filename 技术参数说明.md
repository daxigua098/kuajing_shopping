# 技术参数说明

> 项目名称：跨境电商开店业务分发与代理分账平台  
> 文档版本：V1.0  
> 文档用途：服务器、数据库及基础技术选型与采购参考

---

## 一、总体技术方案

本系统是一个面向公司、顶级代理、子代理和平台管理员的在线业务系统，操作人员通过浏览器登录使用。

系统核心需求包括：

- 多公司、多级代理、人头、店铺和账号管理
- 公司、代理、店铺、人头之间的数据权限控制
- 月度店租核算
- 垫付杂费管理和公司报销
- 代理逐级分配和利润核算
- 支付、对账、导出和报表
- 多语言支持
- 操作日志、审计和数据安全

系统整体采用前后端分离架构，后端先采用模块化单体，暂不采用微服务。

推荐技术组合：

```text
Vue 3 + TypeScript + Spring Boot 3 + MySQL 8 + Redis + 对象存储 + Nginx + Docker
```

系统逻辑架构：

```text
浏览器
  |
  | HTTPS
  v
Nginx
  |
  v
Vue 3 前端系统
  |
  | REST API
  v
Spring Boot 模块化单体
  ├── 登录与权限
  ├── 公司与代理管理
  ├── 人头与店铺管理
  ├── 开店任务管理
  ├── 店铺租金规则
  ├── 杂费与垫付管理
  ├── 月度结算引擎
  ├── 支付与对账
  ├── 导出与报表
  ├── 多语言管理
  └── 操作日志与审计
  |
  v
MySQL 8 + Redis + 对象存储
```

---

## 二、推荐技术栈

| 层级 | 推荐技术 | 说明 |
|---|---|---|
| 前端框架 | Vue 3 + TypeScript + Vite | 适合后台管理系统和多角色操作界面 |
| UI 组件 | Element Plus 或 Ant Design Vue | 提供表格、表单、弹窗、树形组件 |
| 前端状态管理 | Pinia | 管理登录信息、权限、语言等状态 |
| 图表组件 | ECharts | 展示利润、店铺、结算和报表数据 |
| 后端框架 | Java 21 + Spring Boot 3 | 适合复杂业务、事务和长期维护 |
| 安全框架 | Spring Security | 登录认证、RBAC 和接口权限控制 |
| 数据访问 | MyBatis-Plus 或 MyBatis | 便于控制复杂查询和报表 SQL |
| 数据库 | MySQL 8.0.36+ 或 MySQL 8.4 LTS | 满足业务事务、统计和递归查询需求 |
| 数据库迁移 | Flyway 或 Liquibase | 管理数据库版本和升级脚本 |
| 缓存 | Redis 7 | 登录会话、锁、验证码、防重复提交 |
| 定时任务 | Quartz 或 Spring Scheduling | 每月 10 号执行月度核算 |
| 文件存储 | S3、OSS、COS 或 MinIO | 保存人头附件、杂费凭证和导出文件 |
| Excel 导出 | EasyExcel 或 Apache POI | 生成 Excel 报表 |
| TXT 导出 | Java 原生流处理 | 支持分隔符、编码和表头配置 |
| 接口文档 | springdoc-openapi | 前后端接口协作和联调 |
| Web 服务器 | Nginx | HTTPS、静态资源和 API 反向代理 |
| 容器化 | Docker + Docker Compose | 部署应用、Redis 和辅助服务 |
| 日志监控 | Spring Boot Actuator + Prometheus/Grafana | 监控服务和数据库状态 |
| 异步任务 | 初期暂不使用 MQ，后期可选 RabbitMQ | 导出、提醒和报表任务异步化 |

---

## 三、服务器配置建议

以下配置按系统规模划分。实际采购时，应根据公司数量、代理层级、店铺数量和每月结算明细数量进行调整。

| 阶段 | 适用情况 | 应用服务器 | MySQL | Redis | 对象存储 | 带宽 |
|---|---|---|---|---|---|---|
| 开发/演示 | 少量测试数据 | 2核4G | 可与应用同机 | 512M～1G | 50G | 3～5M |
| 小规模上线 | 10～20家公司 | 4核8G | 2核4G，100G SSD | 1G | 100G | 5～10M |
| 推荐上线 | 20～50家公司、1万～3万店铺 | 4核8G或2台4核8G | 4核8G，200G SSD，高可用 | 2G | 100～300G | 10M |
| 业务增长 | 5万店铺以上、百万级月结算明细 | 2台4核8G或2台8核16G | 8核16G，500G SSD，高可用 | 4G | 500G以上 | 10～20M |

### 3.1 推荐正式环境配置

| 项目 | 配置 |
|---|---|
| 应用服务器 | 4核8G，100～200G SSD，5～10M带宽 |
| MySQL | 4核8G，200G SSD，高可用版 |
| Redis | 2G，内网访问 |
| 对象存储 | 100～300G，按量付费 |
| 数据库备份 | 每日全量备份 + Binlog，保留14～30天 |
| 操作系统 | Ubuntu 22.04/24.04 LTS |
| 部署区域 | 马来西亚或新加坡，与应用和用户主要访问地区一致 |

### 3.2 预算有限的试运行方案

| 项目 | 配置 |
|---|---|
| 单台服务器 | 4核8G，200G SSD，5～10M带宽 |
| 同机安装 | Spring Boot + MySQL + Redis + Nginx |
| 对象存储 | 100G |
| 备份 | 每天备份到服务器之外 |

该方案可以用于开发、测试和试运行。正式产生结算数据后，建议尽快将 MySQL 迁移到独立数据库。

### 3.3 服务器采购注意事项

1. 应用服务器建议使用 4核8G 起步，2核4G 适合开发测试。
2. MySQL 正式环境建议 4核8G 起步，不建议使用 2核2G。
3. 数据库磁盘建议使用 SSD 或 ESSD，IOPS 至少达到 3000。
4. 应用服务器和数据库应选择同一个区域、同一个 VPC。
5. 数据库不要开放公网访问，只允许应用服务器通过内网访问。
6. 附件和导出文件优先使用对象存储，降低服务器带宽和磁盘压力。
7. 生产环境建议开启服务器自动快照和数据库自动备份。
8. 操作系统优先选择 Ubuntu LTS、Alibaba Cloud Linux 3 或 TencentOS Server。

---

## 四、MySQL 配置说明

### 4.1 MySQL 版本与购买要求

推荐选择：

- 版本：MySQL 8.0.36+ 或 MySQL 8.4 LTS
- 存储引擎：InnoDB
- 架构：生产环境选择高可用版
- 存储类型：SSD 或 ESSD
- 存储容量：正式环境建议 200G 起步
- 备份：开启自动备份
- Binlog：开启
- 时间点恢复：开启 PITR
- 公网访问：关闭
- 访问方式：只允许应用服务器通过内网访问

不建议使用 MySQL 5.7，也不建议使用已经停止维护的旧版本。

### 4.2 MySQL 4核8G 参考配置

以下配置可作为 MySQL 8.0.36+ 或 MySQL 8.4 的参考：

```ini
[mysqld]
character-set-server = utf8mb4
collation-server = utf8mb4_0900_ai_ci
default-storage-engine = InnoDB

default-time-zone = '+00:00'
transaction-isolation = READ-COMMITTED

max-connections = 300
max-allowed-packet = 64M

innodb_buffer_pool_size = 5G
innodb_buffer_pool_instances = 4
innodb_redo_log_capacity = 2G
innodb_flush_log_at_trx_commit = 1
sync_binlog = 1

binlog-format = ROW
binlog-expire-logs-seconds = 1209600

innodb_io_capacity = 2000
innodb_io_capacity_max = 4000
innodb_print_all_deadlocks = ON

slow-query-log = ON
long-query-time = 1

local-infile = OFF
event-scheduler = OFF
```

如果使用云数据库，部分参数可能需要通过参数组配置，具体以云厂商支持情况为准。

### 4.3 不同规格的 MySQL 参数建议

| MySQL 规格 | Buffer Pool | Redo Log | 最大连接数建议 |
|---|---:|---:|---:|
| 2核4G | 2G | 1G | 200 |
| 4核8G | 5G | 2G | 300 |
| 8核16G | 10～12G | 4G | 500 |
| 16核32G | 20～24G | 8G | 800 |

应用连接池不要配置成几百个连接。Spring Boot 的 HikariCP 建议每个应用实例配置 20～30 个连接。数据库的 `max_connections` 只是全局上限，不代表应用应该建立同等数量的连接。

### 4.4 MySQL 关键参数说明

| 参数 | 说明 |
|---|---|
| `innodb_buffer_pool_size` | InnoDB 核心内存参数，建议为数据库内存的 50%～70% |
| `innodb_flush_log_at_trx_commit` | 设置为 1，保证事务提交后尽量不丢数据 |
| `sync_binlog` | 设置为 1，保证 Binlog 和事务同步 |
| `binlog-format` | 推荐 ROW，便于恢复和追踪数据变更 |
| `default-time-zone` | 建议使用 UTC，业务时区由应用层处理 |
| `transaction-isolation` | 推荐 READ-COMMITTED，减少 Web 系统中的锁等待 |
| `slow-query-log` | 开启慢查询日志，便于优化结算和报表 SQL |
| `local-infile` | 关闭，避免本地文件导入安全风险 |
| `event-scheduler` | 关闭，定时任务由应用系统统一管理 |

---

## 五、Redis 配置建议

Redis 主要用于：

- 登录会话
- 验证码
- 防重复提交
- 月度结算锁
- 接口限流
- 导出任务状态
- 临时缓存

首次购买建议：

| 项目 | 建议 |
|---|---|
| 版本 | Redis 7 |
| 内存 | 2G |
| 高可用 | 正式环境建议开启 |
| 公网访问 | 关闭 |
| 访问方式 | 仅允许应用服务器内网访问 |
| 持久化 | 根据会话和任务要求配置 |
| 用途 | 会话、缓存、锁、限流和状态管理 |

如果预算有限，1G 也可以使用，但正式环境建议选择 2G 或更高规格。

---

## 六、对象存储配置建议

对象存储用于保存：

- 人头身份证、护照、照片和附件
- 店铺资料
- 杂费凭证
- 银行资料附件
- Excel 导出文件
- TXT 导出文件

推荐使用：

- 阿里云 OSS
- 腾讯云 COS
- AWS S3
- 兼容 S3 的 MinIO

建议配置：

- 私有读写
- 不公开 Bucket
- 使用签名 URL 临时访问
- 开启服务端加密
- 设置生命周期规则
- 重要附件保留原文件和操作记录
- 导出文件设置过期自动清理

初始容量可以从 100G 起步，后续按实际文件量扩容。

---

## 七、数据库设计关键要求

### 7.1 字符集

数据库、表和连接统一使用：

```text
utf8mb4
```

推荐排序规则：

```text
utf8mb4_0900_ai_ci
```

这样可以支持中文、马来文、泰文、越南文和特殊字符。

### 7.2 金额字段

金额不要使用 `FLOAT` 或 `DOUBLE`，应使用：

```sql
DECIMAL(20,4)
```

汇率建议使用：

```sql
DECIMAL(20,10)
```

不同币种的金额、汇率和舍入规则需要在应用层统一处理。

### 7.3 代理树设计

代理树不建议只依赖 `parent_id` 递归查询。建议增加代理关系闭包表：

```text
agent_relation
- ancestor_id
- descendant_id
- depth
```

闭包表适合：

- 查询某个代理下面的全部子代理
- 计算代理数据权限
- 统计代理树利润
- 生成下级代理结算数据

MySQL 8 支持递归 CTE，但闭包表在权限校验和批量统计场景中更稳定。

### 7.4 结算规则版本

公司代理规则和代理分配规则必须保留历史版本，建议字段包括：

```text
effective_date
expire_date
version
```

不能直接覆盖历史规则，否则重新核算历史月份时可能产生错误。

### 7.5 月度结算批次

建议增加以下业务表：

```text
settlement_batch
settlement_detail
settlement_snapshot
```

结算批次至少记录：

- 结算月份
- 执行时间
- 执行人
- 规则快照
- 店铺存活快照
- 杂费明细
- 计算结果
- 是否已确认
- 是否已支付
- 异常和重算原因

已经确认的核算单不能直接修改。修正应通过调整单、冲正单或重新生成批次的正式流程处理。

### 7.6 索引建议

重点关注以下字段的索引：

```text
company_id
agent_id
owner_id
shop_id
settle_month
expense_month
status
open_date
close_date
created_at
```

多公司、多代理系统的大部分查询都应带 `company_id` 或代理范围条件，并在数据库层配合应用层数据权限控制。

### 7.7 财务表字段建议

业务表建议统一考虑以下字段：

```text
id
company_id
created_at
updated_at
created_by
updated_by
version
status
```

财务和结算数据不建议物理删除，应使用状态、冲正或调整方式处理。

---

## 八、部署与运维建议

### 8.1 初始部署

推荐初始部署结构：

```text
云服务器
  ├── Nginx
  ├── Vue 3 静态文件
  ├── Spring Boot 应用
  ├── Redis
  └── Docker Compose
          |
          v
云数据库 MySQL
          |
          v
对象存储
```

如果预算允许，MySQL 和 Redis 使用云厂商托管服务，应用服务器只负责 Nginx、Vue 和 Spring Boot。

### 8.2 后续扩展

业务增长后可以扩展为：

```text
负载均衡
  ├── 应用服务器 A
  └── 应用服务器 B
          |
          v
MySQL 高可用主从
          |
          v
Redis 高可用
          |
          v
对象存储
```

导出、报表、提醒等任务可以使用独立 Worker 或消息队列异步执行。

### 8.3 备份要求

- 数据库每日全量备份
- 开启 Binlog
- 备份保留 14～30 天
- 定期验证恢复流程
- 备份文件与生产数据库分离存放
- 重要节点可以进行手工全量备份
- 服务器系统盘也建议每天自动快照

### 8.4 安全要求

- 数据库关闭公网访问
- 使用内网地址连接数据库
- 使用安全组和防火墙限制来源 IP
- 数据库账号按最小权限分配
- 应用账号不能拥有建表、删库和授权权限
- 数据库迁移使用独立账号
- 报表查询可以使用只读账号
- 使用 HTTPS
- 登录密码使用 Argon2id 或 BCrypt 哈希
- 银行卡、身份证等敏感信息加密存储并脱敏展示
- 附件使用私有存储和临时签名地址
- 所有关键业务操作记录操作日志
- 结算、支付、规则修改必须记录操作人和时间

---

## 九、采购清单

### 9.1 推荐正式环境

| 项目 | 配置 |
|---|---|
| 应用服务器 | 4核8G，200G SSD，10M带宽 |
| 操作系统 | Ubuntu 22.04/24.04 LTS |
| MySQL | MySQL 8.0，4核8G，200G SSD，高可用 |
| Redis | 2G，内网访问 |
| 对象存储 | 100～300G，按量付费 |
| 数据库备份 | 每日全量 + Binlog，保留14～30天 |
| 部署区域 | 马来西亚或新加坡 |
| 安全 | HTTPS、VPC、内网访问、安全组、自动备份 |

### 9.2 预算有限的试运行环境

| 项目 | 配置 |
|---|---|
| 单台服务器 | 4核8G，200G SSD，5～10M带宽 |
| 部署内容 | Spring Boot + MySQL + Redis + Nginx |
| 对象存储 | 100G |
| 备份 | 每天备份到服务器之外 |
| 适用阶段 | 开发、测试、试运行 |

---

## 十、技术结论

本项目推荐使用：

```text
Vue 3 + TypeScript + Spring Boot 3 + MySQL 8 + Redis + 对象存储 + Nginx + Docker
```

首次正式上线推荐采购：

```text
应用服务器：4核8G，200G SSD，10M带宽
MySQL：4核8G，200G SSD，高可用
Redis：2G
对象存储：100G
```

架构采用模块化单体，暂不使用微服务。系统设计和数据库设计需要优先保证数据权限、代理树关系、结算规则版本、月度结算批次和财务数据可追溯性。